# 顶部渐隐析出效果移除 + 顶部过渡色统一

## 用户需求

聊天页顶部现有「消息滑到顶部渐渐析出（淡出消失）」的效果，用户要求：
1. 删掉这个渐隐析出效果——消息滑到顶就是普通的被裁掉，不要透明渐变；
2. 顶部（状态栏下方到消息区之间）用与聊天背景相似的颜色自然过渡即可。

背景：iOS 26 状态栏颜色采样方案已上线（html 根元素背景色 = 聊天背景顶部色），
本次是它的配套视觉简化。

## 改动位置（文件：index.html，行号为当前近似值）

### 1. 删除 mask 渐隐（约 259–276 行）
`#chatScroll, .subchat-scroll` 规则里的 `-webkit-mask-image` 和 `mask-image`
两段 linear-gradient **整体删除**。保留该规则的 padding / min-width 等其他声明。
注意 `.subchat-scroll`（子聊天页）与主聊天共用此规则，一起生效，无需另改。

### 2. 顶部过渡遮罩 .chat-top-frost（约 277–286 行 + 1425–1426 行）保留
这是另一个独立元素（导航栏底下那层半透明主题色 scrim，
`color-mix(var(--bg-app) 55%, transparent)` 往下渐透明），它就是用户要的
「顶部背景相似色过渡」，**保留不动**。删除 mask 后它成为顶部唯一的过渡层，
效果即：消息正常滚动到被它覆盖，顶部有一层聊天背景色的柔和压暗，无析出。

若用户验收时觉得顶部过渡太淡/太浓，微调 .ctf-scrim 里 color-mix 的 55%
（加大更浓、减小更淡），只动这一个数字。

### 3. 检查点
- 删除 mask 后，第一条消息滚到顶时应被 .chat-top-frost 自然压住，
  不应出现文字与顶栏图标（返回键、标题）裸叠且无任何过渡的情况；
- `#chatScroll` 顶部 padding（+58px）不要动——它保证静止时首条消息
  不顶到导航栏。

## 明确不要做的
- 不要动 .chat-top-frost / .ctf-scrim 的结构（除非验收后用户要求调浓淡）；
- 不要动底部 composer、safe-area、状态栏采样（setThemeColorMeta）等已上线逻辑；
- 不要动子聊天页以外的其他页面；
- 改完 commit + push 停下，等用户真机验收，不要自行启动浏览器验证。

## 验收（用户真机）
① 聊天消息往上滑，滑到顶部是被背景色过渡层覆盖，而不是逐渐变透明消失；
② 顶部状态栏—导航栏—消息区颜色过渡自然，无突兀分界线；
③ 换 kakao 皮肤（带背景图）和暗色模式各看一遍，过渡层颜色跟随正常。
